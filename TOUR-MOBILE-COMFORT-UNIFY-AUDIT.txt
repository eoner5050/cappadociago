CappadociaGo Tour Detail Deep Mobile Unification — 2026-08-30

Goal
- Use the Göreme Comfort Balloon detail page as the visual reference for all tour detail pages.
- Keep desktop and mobile content identical (responsive layout only; no hidden content).
- Keep the shared CappadociaGo header/footer on every tour page.
- Fix mobile text collapsing into one-letter vertical columns.
- Fix Quick Reservation phone country code handling.

Implemented
1. Added /public/tour-comfort-unified-v2.css as the final, shared tour-detail stylesheet.
2. Loaded that stylesheet at the very end of TourPremiumTheme.astro so it wins over legacy page-specific CSS.
3. Standardized both class systems used by the site:
   - .page/.hero/.gallery/.grid/.content/.side/.booking
   - .tbc-tour-page/.tbc-tour-hero/.tbc-photo-grid-wrapper/.tbc-tour-grid/.tbc-tour-main/.tbc-tour-side/.tbc-booking-card
4. On mobile, the layout is always one full-width content column followed by the price/reservation column.
5. Forced all headings, paragraphs, lists and sections back to horizontal writing mode and normal word wrapping.
6. Calendar popups are constrained to the viewport and raised above all content.
7. intl-tel-input country selector now uses a visible separate dial code, normal national number entry, and a non-fullscreen dropdown.
8. Database-created tours using LiveBookingCard now have an explicit country-code selector and phone number input.
9. Shared SiteHeader and TourFooter remain in use; reservation/availability/Supabase logic is otherwise unchanged.

Static checks
- Direct custom tour pages: all import TourPremiumTheme + SiteHeader + TourFooter.
- Activity/database tours continue through StandardizedPrivateTourPage, which imports the same shared system.
- Mobile content is explicitly visible and no text section is allowed to collapse to a narrow vertical strip.

Build note
- npm install could not finish in the isolated build container due network timeout, so run npm install && npm run build locally before deployment.
