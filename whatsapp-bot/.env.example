PORT=3001
# Must exactly match WHATSAPP_BOT_WEBHOOK_SECRET on Vercel.
WEBHOOK_SECRET=replace-with-a-long-random-secret
# Number that should RECEIVE reservation notifications. Digits only, country code included.
NOTIFY_TO=905401015050
# Authentication files are stored here. Keep this directory persistent on the server.
AUTH_DIR=./auth
