---
import DatabaseTourPage from '../../../components/DatabaseTourPage.astro';
import { getPublishedTour } from '../../../lib/databaseTours';
export const prerender = false;
const { slug = '' } = Astro.params;
const result = await getPublishedTour(slug, 'en');
if (!result) return Astro.redirect('/en/cappadocia-tours/');
---
<DatabaseTourPage lang="en" tour={result.tour} translation={result.translation} />
