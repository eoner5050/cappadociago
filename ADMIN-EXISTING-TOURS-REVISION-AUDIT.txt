CAPPADOCIAGO ADMIN — EXISTING TOURS REVISION AUDIT
Date: 2026-08-30

Implemented:
1. 22 current CappadociaGo tour slugs are generated into src/data/existingTourCatalog.ts from EN/TR/ES tour pages.
2. /api/admin/tours merges existing site tours with database-created tours.
3. Existing tours appear as “Mevcut Site Turu” in /admin/tours and can be opened for revision.
4. First draft/publish creates a database override with the same slug, so the public URL does not change.
5. src/middleware.ts checks published database overrides for /en|tr|es/tours/:slug and rewrites to the managed database page.
6. Draft revisions do NOT replace the public static page until published.
7. “Revizyonu Geri Al” deletes the override and restores the original static tour and product defaults.
8. Existing automatic TR/EN/ES translation workflow is preserved.
9. New tours continue to publish normally through the database dynamic route.

Existing tour count detected: 22 (each has EN/TR/ES catalog data).

Build note:
- npm run build prebuild/sitemap completed.
- Astro build could not run in this workspace because node_modules is intentionally not packaged here (astro: not found).
- Run npm install && npm run build locally before deployment.
